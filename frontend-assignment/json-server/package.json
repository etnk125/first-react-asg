{
  "name": "json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 9000 --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
